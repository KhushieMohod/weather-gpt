apiVersion: v1
kind: Service
metadata:
  name: weathergpt-service
  namespace: weathergpt
  labels:
    app.kubernetes.io/name: weathergpt
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: weathergpt-backend
